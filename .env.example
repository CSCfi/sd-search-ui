VITE_AUTH_BYPASS="false"                       # Bypass auth check for local development
BACKEND_URL="http://host.docker.internal:8000" # Backend URL for local development (used by the backend proxy)
VITE_DOD_ENDPOINT_URL=""                        # Placeholder — DoD submission target, TBD pending SDA auth spec
VITE_REMS_URL=""                                # REMS base URL — set per environment (test/prod)
